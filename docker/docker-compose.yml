services:
  # ── PostgreSQL ──────────────────────────────────────────────────────────────
  db:
    image: postgres:15
    restart: unless-stopped
    env_file:
      - ../.env
    environment:
      POSTGRES_DB: english_trainer
      POSTGRES_USER: trainer_user
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U trainer_user -d english_trainer"]
      interval: 5s
      timeout: 3s
      retries: 15
      start_period: 10s

  # ── Web App (FastAPI) ───────────────────────────────────────────────────────
  app:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      - ../static:/app/static
    env_file:
      - ../.env
    environment:
      DATABASE_URL: postgresql://trainer_user:${POSTGRES_PASSWORD}@db:5432/english_trainer
    depends_on:
      db:
        condition: service_healthy

volumes:
  pgdata:
    name: english_trainer_pgdata
